# 📝 Buylist Bot - Telegram бот для преобразования рукописных списков продуктов

Асинхронный Telegram бот на Python, который принимает изображения и PDF файлы с рукописными списками продуктов, извлекает из них текст с помощью Datalab API и создает красивое изображение с печатным текстом.

## ✨ Возможности

- 📸 Прием изображений списков продуктов (JPEG, PNG, WebP)
- 📄 Прием PDF файлов со списками продуктов
- 🔍 Извлечение текста из рукописных изображений и PDF через Datalab Marker API
- 🔄 Переключаемый OCR: облачный Datalab или локальный PaddleOCR (paddlepaddle 3.0.0)
- 🎨 Автоматическое создание красивых изображений с печатным текстом
- ⚡ Полностью асинхронная архитектура на aiogram 3.x
- 🚫 Файлы не сохраняются на сервере

## 🛠 Технологии

- **aiogram 3.x** - современная асинхронная библиотека для Telegram ботов
- **Datalab Marker API** - API для извлечения текста из изображений
- **Pillow (PIL)** - генерация изображений
- **aiohttp** - асинхронные HTTP запросы
- **python-dotenv** - управление переменными окружения
- **PaddleOCR (опционально)** - локальный OCR на базе paddlepaddle==3.0.0

## 📋 Требования

- Python 3.8+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- Datalab API Key (получить на [datalab.to](https://datalab.to)) — нужен только в режиме `OCR_PROVIDER=datalab`
- Для локального режима `OCR_PROVIDER=paddle`: установленные пакеты `paddlepaddle==3.0.0` и `paddleocr` (см. ниже)

## 🚀 Установка и запуск

### 1. Клонируйте репозиторий или скачайте файлы

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Настройте переменные окружения

Создайте файл `.env` в корне проекта (можно скопировать из `.env.example`):

```env
BOT_TOKEN=your_telegram_bot_token_here
DATALAB_API_KEY=your_datalab_api_key_here
OCR_PROVIDER=datalab   # или paddle
PADDLE_OCR_LANG=ru     # язык для PaddleOCR
```

**Как получить BOT_TOKEN:**
1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен

**Как получить DATALAB_API_KEY:**
1. Зарегистрируйтесь на [datalab.to](https://datalab.to/auth/sign_up)
2. Перейдите в настройки API
3. Создайте новый API ключ

### Переключение OCR провайдера

- По умолчанию используется облачный Datalab (`OCR_PROVIDER=datalab`).
- Чтобы перейти на локальный PaddleOCR:
  1. Установите зависимости: `pip install paddlepaddle==3.0.0 paddleocr==2.7.3`
  2. Установите переменные окружения: `OCR_PROVIDER=paddle` и при необходимости `PADDLE_OCR_LANG=ru`
  3. Учтите: PaddleOCR не обрабатывает PDF напрямую — конвертируйте PDF в изображения.

### 4. Запустите бота

```bash
python bot.py
```

## 📖 Использование

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start` для начала работы
3. Напишите список продуктов от руки на листе бумаги
4. Сфотографируйте список или сохраните как PDF
5. Отправьте фотографию или PDF файл боту
6. Получите красивое изображение с печатным текстом!

**Поддерживаемые форматы:**
- Изображения: JPEG, PNG, WebP (до 10 MB)
- PDF файлы: до 50 MB

## 💡 Советы для лучшего результата

- Используйте хорошее освещение при фотографировании
- Убедитесь, что текст четкий и читаемый
- Держите камеру параллельно листу
- Избегайте теней и бликов
- Максимальный размер изображения: 10 MB
- Максимальный размер PDF: 50 MB

## 📁 Структура проекта

```
buylist_bot/
├── bot.py                 # Основной файл запуска бота
├── config.py              # Конфигурация и переменные окружения
├── requirements.txt       # Зависимости проекта
├── .env.example           # Пример файла с переменными окружения
├── .gitignore            # Игнорируемые файлы для git
├── handlers/             # Обработчики сообщений
│   ├── __init__.py
│   ├── start_handler.py   # Обработчик /start
│   ├── help_handler.py   # Обработчик /help
│   └── photo_handler.py  # Обработчик фотографий
└── services/             # Сервисы
    ├── __init__.py
    ├── datalab_service.py    # Работа с Datalab API
    └── image_generator.py    # Генерация изображений
```

## 🔧 Настройки

В файле `config.py` можно изменить следующие параметры:

- `IMAGE_MAX_SIZE` - максимальный размер изображения (по умолчанию 10 MB)
- `PDF_MAX_SIZE` - максимальный размер PDF файла (по умолчанию 50 MB)
- `POLLING_INTERVAL` - интервал между проверками статуса обработки (по умолчанию 2 секунды)
- `MAX_POLLS` - максимальное количество попыток проверки статуса (по умолчанию 300)

## 📝 Логирование

Бот ведет логи в файл `bot.log` и выводит их в консоль. Уровень логирования можно изменить в `bot.py`.

## ⚠️ Важные замечания

- Изображения обрабатываются в памяти и не сохраняются на диск
- Результаты обработки Datalab API удаляются через час после завершения
- Для работы бота требуется активное интернет-соединение
- Datalab API может иметь ограничения по количеству запросов (зависит от тарифа)

## 🤝 Вклад в проект

Если вы хотите улучшить бота, создайте issue или pull request!

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 🔗 Полезные ссылки

- [Документация aiogram](https://docs.aiogram.dev/)
- [Документация Datalab API](https://documentation.datalab.to/)
- [Документация Pillow](https://pillow.readthedocs.io/)

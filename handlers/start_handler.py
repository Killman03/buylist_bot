"""Обработчик команды /start."""
import logging

from aiogram import Router
from aiogram.filters import CommandStart
from aiogram.fsm.context import FSMContext
from aiogram.types import Message

logger = logging.getLogger(__name__)

router = Router(name="start_handler")


@router.message(CommandStart())
async def cmd_start(message: Message, state: FSMContext) -> None:
    """Обработчик команды /start."""
    # Сбрасываем состояние
    await state.clear()
    
    welcome_text = (
        "👋 Привет! Я бот для преобразования рукописных списков продуктов.\n\n"
        "📝 Как использовать:\n"
        "1. Напишите список продуктов от руки на листе бумаги\n"
        "2. Сфотографируйте список или сохраните как PDF\n"
        "3. Отправьте фотографию или PDF файл мне\n"
        "4. Проверьте извлеченный текст и при необходимости отредактируйте\n"
        "5. Подтвердите и получите красивый печатный список!\n\n"
        "📸 Отправьте мне фотографию или PDF файл вашего списка продуктов!"
    )

    await message.answer(welcome_text)
    logger.info(f"Пользователь {message.from_user.id} запустил бота")

